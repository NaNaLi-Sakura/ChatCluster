# cmake_minimum_required(VERSION 3.20) # 要求cmake最低的版本号

# project(01_Test) # 定义当前工程名字

# 配置编译选项
# set(CMAKE_CXX_FLAGS ${CMAKE_CXX_FLAGS} -g)

# 配置头文件搜索路径
# include_directories()
# 配置库文件搜索路径
# link_directories()

# 指定搜索的子目录
add_subdirectory(./src)